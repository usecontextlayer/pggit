# Mapping — `realrepo` lens: `breakage/` repro scripts → house testing patterns

6 source scripts assigned, 6 converted. Every one routed to **perf** (`perf/breakage/`): each drives a REAL local repository handed in at run time, which is the routing rule's second trigger ("the script needs a real EXTERNAL repo"), and three of them additionally score a MEASURED size ratio. None can become a vitest e2e test, because there is no committed fixture that can stand in for the corpus — the same reason `perf/delta-corpus.ts` and `perf/delta-probe.ts` live in `perf/`.

| source (`breakage/…`) | destination | kind | exact property it asserts / measures | expected current state |
|---|---|---|---|---|
| `realrepo--branch-order.ts` | `perf/breakage/realrepo--branch-order.ts` | perf | Pushes every ref of a real repo ONE AT A TIME with a `repack()` between each push, in forward AND reverse order (D4: anchors freeze over a partial DAG, a later branch introduces new same-path pairs). Per order: push accept/reject parity with a `file://` oracle receiving the identical per-ref sequence, `git fsck --strict` clean, object set + ref listing byte-identical to the oracle, and `git verify-pack -v` chain depth ≤ `--max-depth` (default 1, the D2/D9 structural bound) as the CLIENT measures it. Cross-order: the two orders must serve identical object sets. Exit 1 on any finding. | perf-diagnostic (correctness gate on real history; the source's own verdict line claims exit 0 = clean, so any finding here is new) |
| `realrepo--cleanup.ts` | `perf/breakage/realrepo--cleanup.ts` | **utility, not a test** | Asserts and measures nothing. Enumerates `t_*` schemas on the shared Postgres, attributes them by `repos.name` prefix (`realrepo/`, `races/`, `border/`, `warm/`, `sizecheck/` — the repo-id prefixes the five harnesses push under), reports, and drops ONLY those under `--drop`. Everything unattributable is left alone (the instance is shared with other sessions). | n/a — teardown for runs that died before their own `finally` |
| `realrepo--differential.ts` | `perf/breakage/realrepo--differential.ts` | perf | Wire-level differential on a real repo against a `file://` bare oracle replaying the identical push. **full**: pre/post-repack mirror clones are exact and the second repack writes nothing. **size**: identical no-have v2 requests produce exact canonical transmitted OIDs before raw response PACK bytes are scored against `git gc --aggressive`. **shapes**: every named branch/tag/blobless/lazy/exact-OID fixture must exist before its result counts; shallow fails cleanly; post-GC clones remain exact. **orphan**: a proven divergent denied push creates reclaimable objects that never leak. **replay**: mixed repacked/skipped rounds compare exact refs and objects at every checkpoint and through a fresh final clone. | perf-diagnostic; missing fixtures, counters, raw packs, or canonical coverage abort unscored. |
| `realrepo--drain-races.ts` | `perf/breakage/realrepo--drain-races.ts` | perf | Three conditions created by directly driving the candidate serialized maintenance sequence: **R1** two exact clients of one unchanged state write byte-identical packs; **R2** a clone starts only after committed partial encoding coverage is observed while repack remains unsettled; **R3** a push starts at that same proven seam and a second repack converges to complete coverage. Every clone is fsck-clean and exact against a `file://` oracle. The probe does not claim the still-deferred production integration invokes repack. | perf-diagnostic; an unobserved overlap is a prerequisite failure, never a green race. |
| `realrepo--incremental-fetch-size.ts` | `perf/breakage/realrepo--incremental-fetch-size.ts` | perf | Replays a real repo checkpoint by checkpoint (`--step`, default 50), repacking pggit and `git gc`-ing the canonical oracle each round, then captures each side's raw protocol-v2 sideband-1 PACK. Canonical `index-pack` proves the exact transmitted OID set; warm rounds additionally require appended thin-pack bases, so the D8′ client-have path cannot be skipped. Verdict: warm-total pggit/git raw-byte ratio vs `--max-ratio` (default 3.0), with per-round and cold-clone ratios printed. | Current D8′ performance gate; missing raw packs, exact-OID coverage, or thin-base evidence is a prerequisite failure, never a favorable zero. |
| `realrepo--serve-size-vs-git.ts` | `perf/breakage/realrepo--serve-size-vs-git.ts` | perf | Push and repack a real mirror, prove mirror-clone refs/OIDs/object bytes/fsck exactly match canonical Git, send identical no-have v2 requests, and require identical transmitted OIDs. Raw response PACK bytes determine the pggit/git ratio; separately indexed copies provide strict `verify-pack -v` per-type attribution without treating client storage as wire evidence. | RED on the motivating corpus when the raw ratio exceeds `--max-ratio`; missing delta coverage or canonical state aborts unscored. |

## Helper created alongside (not one of the 6 assigned scripts)

| file | what it is |
|---|---|
| `perf/breakage/_realrepo-util.ts` | Shared strict boundaries for the harnesses: numeric flags, scratch/mirror handling, outcome comparison, exact OID inventory and stored-tier coverage, raw v2 PACK capture for pggit and canonical upload-pack, and canonical raw-pack indexing/OID validation. No threshold lives here. |

## Conversion notes

- **Mirror-clone-before-touch is now enforced in one place.** Every harness resolves its corpus through `prepareMirror`, which does `git clone --mirror --no-hardlinks -q <source> <scratch>/source.git` (the discipline `perf/delta-corpus.ts` documents) and hands back the private clone. The harnesses push from it, `git config uploadpack.*` into it (the differential), and `git gc` the oracles built out of it, so the source checkout — `pggit`, `trove`, `web`, a customer mirror — stays byte-for-byte pristine. The sources took a pre-made `--mirror=<bare.git>` and worked on it directly; both spellings are accepted (`--repo=` preferred, `--mirror=` still honoured), and both now go through the clone.
- **The original conversion was mechanical; the 2026-08-20 evidence pass was deliberately not.** It retained the fixtures, thresholds, `file://` oracle, and cleanup prefixes while making every scored arm prove its named preconditions and exact canonical coverage first. Size gates now score raw response PACKs because client-indexed packfiles are storage evidence, not wire evidence.
- **2026-08-20 evidence amendment.** Client-written packfiles are retained only as storage evidence because Git may fix thin packs or otherwise rewrite them while indexing; raw response PACKs are the wire-size authority.
- **Repo-id prefixes are load-bearing** and unchanged: `border/…` (branch-order), `races/…` (drain-races), `realrepo/…` (differential), `warm/…` (incremental-fetch-size), `sizecheck/…` (serve-size-vs-git). `realrepo--cleanup.ts` attributes schemas by exactly these prefixes; renaming one silently orphans its schemas on the shared Postgres.
- **The conversion did not modify its source scripts.** The later evidence pass also shared canonical fixture and pack parsers through `src/testing/git-fixtures.ts` so the perf harnesses and tests enforce one grammar.
